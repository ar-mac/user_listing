.row
  .col-md-4.col-md-offset-4
    %h1= t('projects.title.new')

    -# obsolete `partial` word, by default render 'string' in templates renders partial `string`, in layouts is looking for a template
    = render partial: 'form'

    = link_to 'Back', users_path
